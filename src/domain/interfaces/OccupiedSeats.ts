interface OccupiedSeats {
  screeningId: string;
  screeningTime: Date;
  movie: string;
  auditorium: string;
  occupiedSeats: string;
  occupied: number;
  total: number;
  occupiedPercent: string;
}